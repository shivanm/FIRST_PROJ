<%= form_for(@progress_report_user) do |f| %>
  <% if @progress_report_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@progress_report_user.errors.count, "error") %> prohibited this progress_report_user from being saved:</h2>

      <ul>
      <% @progress_report_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
